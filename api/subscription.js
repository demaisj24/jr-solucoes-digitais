const SUPABASE_URL='https://uxmlmyhiagjefuufanyg.supabase.co';const KEY=process.env.SUPABASE_SERVICE_ROLE_KEY||'';
function json(res,s,b){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');return res.status(s).json(b)}
async function user(req){const a=String(req.headers.authorization||'');if(!a.startsWith('Bearer ')||!KEY)return null;const r=await fetch(`${SUPABASE_URL}/auth/v1/user`,{headers:{apikey:KEY,Authorization:a}});if(!r.ok)return null;return r.json()}
export default async function handler(req,res){if(req.method!=='GET')return json(res,405,{error:'Método não permitido.'});try{const u=await user(req);if(!u?.id)return json(res,401,{error:'Faça login.'});const r=await fetch(`${SUPABASE_URL}/rest/v1/subscriptions?user_id=eq.${encodeURIComponent(u.id)}&select=plan_code,status,price_cents,trial_started_at,trial_ends_at,current_period_start,current_period_end,cancel_at_period_end,canceled_at,provider_checkout_url&limit=1`,{headers:{apikey:KEY,Authorization:`Bearer ${KEY}`}});const p=await r.json().catch(()=>null);if(!r.ok)return json(res,500,{error:'Não foi possível consultar a assinatura.'});return json(res,200,{subscription:p?.[0]||null})}catch(e){console.error(e);return json(res,500,{error:'Erro interno.'})}}
